[project]
name = "deep-q-learning"
version = "0.1.0"
description = "Deep Q-Learning agent for CartPole"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "gymnasium",
    "matplotlib",
    "torch",
]
